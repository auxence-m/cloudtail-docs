## cloudtail docs

Generate documentation for cloudtail

### Synopsis

The docs command generates a complete documentation for cloudtail. 

By default, documentation files are be generated iin the ./docs directory.
You can override the output directory using the --dir flag.

Markdown files are generated by default. Use the --format flag to specify a different output format.
The supported output formats are markdown, man, rest and yaml.

```
cloudtail docs [flags]
```

### Options

```
  -d, --dir string      Destination directory for the documentation
  -f, --format string   Format of the documentation (markdown, man, rest, yaml)
  -h, --help            help for docs
```

### SEE ALSO

* [cloudtail](index.md)	 - cloudtail displays or tail logs from Google Cloud Logging

